# let queryfn when relations

FEATURE: one-to-one parent
background:
 configure loadFKs = true
thenType: struct

# --- Multiple ---
---
title: function
given:
 type Customer struct {id int unique, relation addr Address many parent } end
 type Address struct {id int unique, relation cust Customer optional one } end

  insert Customer {id: 55 }
  insert Customer {id: 56 }
when:
  let x = Customer[true].id.max()
then:
value:a:int:56

